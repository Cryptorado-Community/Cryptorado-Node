# CRYPTORADO_NEBULA_CONFIG defines which file within the config directory
# contains the configuration. Change this to "host.lighthouse.yaml" if you're
# running a lighthouse node.
CRYPTORADO_NEBULA_CONFIG=host.yml

# CRYPTORADO_NEBULA_PORT defines which port nebula will listen on. Useful if you
# have other non-Cryptorado nebula network running on your host machine.
CRYPTORADO_NEBULA_PORT=4242
